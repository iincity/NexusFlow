# 2026-08-24 本机发行验证记录

## 范围

- 验证 worktree：`E:\NexusFlow-release-verify-20260824c`
- 根仓库基线：`634210f test: stabilize release UDP relay coverage`（本记录与格式化修复随后提交）
- Tunnel gitlink：`e2faa3ab06db86fb7377717c08032c5d5c50082e`
- 验证日期：2026-08-24
- Hosted CI：按当前决定未执行；不影响本记录的本机结论。

`.release-target/` 是 `verify-release.ps1` 产生的未跟踪本机输出，未纳入提交。

## 已通过的本机验证

| 范围 | 命令/检查 | 结果 |
| --- | --- | --- |
| 发布完整性 | `tools/verify-release.ps1` | 通过；SBOM 连续生成 SHA 一致。|
| Pro 制品锁定 | 下载、版本与 SHA256 检查 | 通过：`1.4.9+67`，`fe84fc807efadc6f617c5c9ce41be656c471f39f837ab0ae66815af9f099602c`。|
| Tunnel Windows | workspace tests、locked check、Clippy、relay process E2E、UDP 并发重复 10 次 | 通过；workspace 112/112。|
| Tunnel WSL Ubuntu 24.04 | `cargo test --locked --workspace`、`cargo fmt --check`、`cargo check --locked --workspace`、`cargo clippy --locked --workspace --all-targets -- -D warnings` | 通过；99 个 unit/integration 测试通过。|
| RustDesk Client | `cargo check --locked -p rustdesk --features nexus-native-tunnel`、`cargo test --locked -p nexus_client`、`cargo fmt -p nexus_client --check` | 通过；`nexus_client` 74/74。|
| RustDesk Server | `cargo check --locked --features nexus-native-tunnel --bin hbbs`、`cargo fmt --check`、native registration process test | 通过。|
| Control Plane Windows | Tunnel Access 21 passed/1 ignored；Native registration 4 passed/1 ignored；`websocket_` 3 passed；`nexus-web-relay` 3 passed；`cargo fmt --check`；`cargo check --locked --lib`；all-features Clippy | 通过。|
| Control Plane WSL Ubuntu 24.04 | `cargo test --locked --lib transport::http::web_gateway_routes::tests::websocket_ -- --test-threads=1` | 3/3 通过。|
| 依赖剔除 | Client 与 Control Plane 的正常/构建依赖树检索 `nexus-tunnel`、`frp-rs`、`nexus-frp-runtime` | 未发现生产依赖。|

## 已发现且非代码变更的验证缺口

1. 首次 WSL 默认 `Ubuntu-24.04` 实例曾出现 `Wsl/Service/CreateInstance/E_FAIL`；现已使用运行中的 `NexusFlow-Test-Ubuntu-24.04-D`（Ubuntu 24.04.4, WSL2）完成 Linux 验证。
2. 正式 Supabase/Auth/Storage/TLS 与 RustDesk Pro 真实设备验收无法由本机模拟替代，已转为开发者执行的 [外部环境验收测试用例](外部环境验收测试用例.md)。

## 结论

除外部正式环境与真实 Pro 数据面验收外，可在当前 Windows + WSL Ubuntu 24.04 环境运行的发布、编译、静态检查、协议、Relay、WebSocket 和回归验证已完成并通过。该结论不等同于正式发布批准；正式批准仍须执行外部环境用例。
